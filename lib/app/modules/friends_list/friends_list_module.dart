import 'package:flutter/material.dart';

class FriendsListModule extends StatelessWidget {
  const FriendsListModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData.dark(),
      home: Scaffold(

      ),
    );
  }
}