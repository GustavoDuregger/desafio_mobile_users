import 'dart:convert';

import 'package:flutter_test/flutter_test.dart';

import '../../../../fixtures/fixture_reader.dart';

void main(){
/*
  test('Should convert a json to a UserModel with the correct values', () async {
    final json = jsonDecode(fixture("user_response.json"));
    final result = UserModel.fromJson(json);

    expect(result.name, json['name']['first'] + ' ' + json['name']['last']);
    expect(result.picture, json['picture']['large']);
    expect(result.gender, json['gender']);
  });
*/
}